# CreatorMatch Walkthrough

I have generated the complete source code for your **CreatorMatch** MVP using Flutter.

## 📂 Project Structure

- **`lib/main.dart`**: The entry point. Sets up the "Dark Mode" theme and routes.
- **`lib/screens/brand_dashboard.dart`**: The core logic. Contains the **Swipe Deck** and the **Recommendation Algorithm**.
- **`lib/widgets/swipe_card.dart`**: The visual card component. Handles the "Scroll Up" gesture detection.
- **`lib/models/creator_models.dart`**: Data models for Profiles and Tags.

## 🚀 How to Run

Because I could not run `flutter create` in this environment, you must initialize the native project files yourself:

1.  Open your terminal.
2.  Go to the project: `cd "Coding Projects/creator_match"`
3.  Run: `flutter create .`
4.  Run: `flutter run`

## 🧪 Verification
Since I cannot run the simulator here, please verify the following on your device:
1.  **Swipe Gestures**: Ensure smooth left/right swiping.
2.  **Scroll Up**: Test that dragging the card "Up" allows you to see the Portfolio.
3.  **Shortlisting**: Swipe Right and check the console/SnackBar for "Shortlisted [Name]".

## 🔮 Next Steps (Supabase)
Currently, the app uses `MockDataService` to demonstrate the UI. To connect Supabase:
1.  Create a project at [supabase.com](https://supabase.com).
2.  Run the SQL script (I can provide this) to create `profiles` table.
3.  Uncomment the `Supabase.initialize` code in `main.dart`.
